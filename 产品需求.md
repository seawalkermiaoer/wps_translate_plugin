
**WPS 极速AI智能翻译插件 (V1.0 MVP) 技术方案设计书**

**项目概述:**
本项目旨在开发一款基于 WPS Web Add-in 框架的智能翻译插件。该插件提供一键式的全文档中译英功能，其核心竞争力在于利用大语言模型（LLM）实现“像人类编辑一样”的翻译质量，同时在不破坏文档原有排版的前提下，精准保留段落内的加粗、斜体、颜色和超链接等富文本格式。V1.0 版本将严格聚焦于纯文本段落的处理，暂时排除表格、公式和浮动对象的干扰，以确保核心算法的稳定性与高效性。

**系统架构总览:**
为了保证客户端的轻量化以及核心业务逻辑的安全，本系统采用经典的三层架构设计。这种架构能够有效隔离 WPS 客户端的复杂 DOM 操作与大模型的网络请求，提升系统的整体容错率。

第一层是位于用户电脑端的 WPS 客户端插件。它主要由 HTML、CSS 和 JavaScript 构成，负责渲染侧边栏或顶部功能区面板。其核心职责是通过 WPS 提供的 JSAPI 遍历文档结构，将复杂的文档对象模型提取并序列化为带有自定义标签的字符串，随后通过 HTTP 请求发送给后端。在接收到翻译结果后，它还需要负责将带有标签的字符串反序列化，并精准备地写回原文档。

第二层是后端中间件服务。该服务建议采用 Node.js 或 Python 构建，作为连接客户端与大模型的桥梁。它的主要职责包括接收客户端发来的序列化文本、进行用户鉴权、执行限流策略（Rate Limiting），并将长文档拆分为适合大模型处理的请求块（Chunk）。此外，中间件还负责向大模型注入 System Prompt，并在模型返回异常时执行重试机制。

第三层是底层的大语言模型服务。考虑到我们需要模型具备极强的 XML 标签遵循能力以及地道的英语表达语感，在2026年的技术背景下，强烈建议优先接入 Claude 3.5 Sonnet 或 GPT-4o 等顶级模型的 API。它们将负责解析带有格式标签的中文内容，并在输出英文时，将这些标签准确无误地包裹在对应的英文词汇上。

**核心工作流与交互逻辑:**
当用户在 WPS 顶部菜单栏点击“全文翻译”按钮时，系统将启动一个全自动的异步处理流程，为用户提供“隐形翻译员”般的视觉体验。

整个过程从初始化选区和进度条开始。客户端插件会首先弹出一个悬浮的进度条UI，并调用 WPS JSAPI 锁定当前文档的状态。为了防止意外损坏，系统会自动开启一个 `UndoRecord` 撤销记录区间。随后，程序获取文档的总段落数（`Document.Paragraphs.Count`），并开始从头到尾的遍历。

在遍历过程中，程序会通过条件判断主动过滤掉复杂元素。当检测到当前段落位于表格内、或者是页眉页脚、或者是图片环绕文本时，程序会直接跳过该段落，不做任何处理。对于普通的正文段落，程序会将其打包成一个处理批次（Batch）发送至后端。

当后端返回带有格式标签的英文结果后，客户端插件会实时更新文档内容。此时，用户在屏幕上会看到中文段落逐个被替换为英文，且原本红色的字体或加粗的词汇在英文中依然保持原样。随着文档的不断向下滚动，进度条逐渐填满。整个过程结束后，用户可以选择接受结果，或者按下快捷键一键恢复到翻译前的最初状态。

**格式保留引擎机制:**
这是整个技术方案中最具挑战性、也是最具价值的核心模块。为了解决中英文语序不同导致的格式错位问题，我们采用“标签序列化与反序列化（Serialization & Deserialization）”算法。

在序列化阶段（前端），我们需要将 WPS 底层的 `Run` 对象转换为纯文本标签。WPS 中的一个段落是由多个具有相同格式的字符集合组成的。程序需要遍历段落内的每一个字符或词组，提取其格式属性（如 `Font.Bold`、`Font.Color`）。如果发现特殊格式，程序会在内存中生成一个唯一的占位符 ID（例如 `<r1>`），并将该格式的具体属性存储在一个 JSON 字典中。最终，一段复杂的 WPS 文本会被转换为诸如“使用<r1>云计算</r1>技术”的扁平化字符串，连同样式字典一起发给后端。

在大模型处理阶段（后端），提示词（Prompt）的设计至关重要。必须通过 System Prompt 明确告知大模型这些 XML 标签的含义与处理规则。以下是一个经过验证的 Prompt 设计范例：

```python
system_prompt = """
You are an expert bilingual translator (Chinese to English).
Your task is to translate the user's text while STRICTLY preserving the XML-like formatting tags.

RULES:
1. The input contains tags like <r1>word</r1>, which represent formatting (e.g., bold, hyperlinks).
2. Translate the text naturally into English, adapting the sentence structure as needed.
3. You MUST wrap the exact translated English equivalent word/phrase with the corresponding tags.
4. DO NOT change the tag names (e.g., keep <r1> as <r1>).
5. DO NOT drop or invent any tags.

Example Input: 我们建议使用<r1>云计算</r1>来降低成本。
Example Output: We recommend using <r1>cloud computing</r1> to reduce costs.
"""
```

在反序列化阶段（前端），插件接收到大模型返回的诸如“We recommend using <r1>cloud computing</r1> to reduce costs.”的字符串后，需要利用正则表达式提取出纯文本和标签位置。随后，程序首先将纯文本“We recommend using cloud computing to reduce costs.”通过 `Range.Text` 写入当前段落，接着根据标签的位置索引计算出“cloud computing”在这段话中的起始和结束位置，最后通过 `Range.Characters(start, length).Font.Bold = true` 的方式，将格式精准地重新涂抹上去。

**文档分块与上下文调度策略:**
对于动辄几十页的长文档，我们无法将其一次性发送给大模型。这不仅会面临 Token 数量超限的问题，还会导致大模型在处理过长文本时出现“遗忘标签”或“输出截断”的幻觉。因此，我们必须引入智能的分块（Chunking）与滑动窗口上下文机制。

在分块策略上，系统不能简单地按字数一刀切，而必须以“段落（Paragraph）”为最小原子单位进行切割，以保证语义的完整性。为了计算每一批次最优的处理段落数，我们可以在后端中间件中引入一个动态计算公式来决定分块大小。假设我们需要确保每次请求不超过大模型的最佳处理视口，我们可以使用以下数学模型来计算单次请求的最优字符数上限：

$$ C_{opt} = \min\left( \frac{T_{max} - T_{sys} - T_{ctx}}{R_{exp}}, C_{limit} \right) $$

在这个公式中，$C_{opt}$ 代表单次请求最优的中文字符数量；$T_{max}$ 是大模型单次请求允许的最大 Token 数限制；$T_{sys}$ 是系统提示词占用的 Token 数量；$T_{ctx}$ 是注入的上下文历史占用的 Token 数量；$R_{exp}$ 是中文字符到 Token 的预估膨胀率（通常在主流大模型中约为 1.5 到 2.0）；而 $C_{limit}$ 则是为了保证响应速度和降低标签错乱风险而人为设定的绝对字符上限（例如 1500 字）。

在上下文调度方面，为了保证整篇文档在专业术语、人名和时态上保持一致，系统在发送当前处理批次（Batch N）时，会自动提取上一批次（Batch N-1）的英文翻译结果，将其作为背景上下文（Context）附加在 Prompt 中，并明确告知大模型：“以下是前文的翻译结果，仅供统一术语和语境参考，无需翻译此部分”。

**数据接口契约:**
前后端之间的通信采用 RESTful API 标准，数据交换格式为 JSON。为了保证格式字典和文本的精准对应，我们需要定义一个严谨的接口契约。以下是客户端发送给后端的请求体（Request Body）结构示例：

```javascript
// POST /api/v1/translate/document-chunk
{
  "documentId": "doc_8837482",
  "chunkIndex": 3,
  "context": "Previous translated text goes here for context...",
  "payload": [
    {
      "paragraphId": "p_001",
      "text": "本协议由<r1>甲方</r1>与<r2>乙方</r2>共同签署。",
      "styleDictionary": {
        "r1": { "bold": true, "color": "#FF0000" },
        "r2": { "bold": true, "color": "#0000FF", "underline": true }
      }
    }
  ]
}
```

后端在处理完成后，将返回包含翻译文本和校验状态的响应体（Response Body）：

```javascript
// Response 200 OK
{
  "status": "success",
  "chunkIndex": 3,
  "results": [
    {
      "paragraphId": "p_001",
      "translatedText": "This agreement is jointly signed by <r1>Party A</r1> and <r2>Party B</r2>.",
      "tagValidation": true 
    }
  ]
}
```

**异常处理与降级方案:**
在自动化处理全文档的过程中，网络波动或大模型的不稳定输出是不可避免的。为了保证整个翻译进程不被轻易中断，我们需要在系统中建立完善的容错与降级机制。

如果后端调用大模型 API 时发生超时或网络错误，中间件应自动执行指数退避重试（Exponential Backoff Retry），最多尝试三次。如果大模型返回的英文文本中丢失了标签（例如原文有 `<r1>`，但返回的英文中没有），后端校验机制（Tag Validation）将捕捉到这一异常。此时，后端会触发局部重试，在 Prompt 中增加“严厉警告”要求模型找回丢失的标签。

如果经过多次重试后，大模型依然无法正确生成包含标签的译文，系统将触发降级策略。为了保证文档不被损坏，针对该特定段落，插件将放弃格式保留功能，直接写入纯文本的英文翻译，并在该段落末尾添加一个不可见的批注或特定背景色，以便用户在通读全文时进行人工复核。这种“宁失格式，不丢内容且不崩溃”的策略，是保障 V1.0 MVP 顺利运行的最后一道防线。